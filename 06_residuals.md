# 残差（Residuals）

回帰分析などの統計モデルにおいて、実際に観測されたデータ（実測値）と、モデルから予測されたデータ（予測値）のズレを示す指標です。

## 数式

$$
e_i = y_i - \hat{y}_i
$$

## 記号の意味

- $e_i$ : $i$番目のデータにおける残差
- $y_i$ : $i$番目のデータの観測値（実測値）
- $\hat{y}_i$ : $i$番目のデータの予測値

## 概要

残差（$e$）は、実際の観測値からモデルの予測値を引き算することで求められます。モデルが現実のデータをどれくらい正確に説明できているか（当てはまりの良さ）を評価するために非常に重要な概念です。

残差が全体的に0に近ければ近いほど、モデルの予測精度が高いことを意味します。代表的な回帰分析の手法である「最小二乗法」では、すべてのデータにおける「残差の2乗の和（残差平方和）」が最も小さくなるように、回帰式（直線の傾きや切片）を決定します。
